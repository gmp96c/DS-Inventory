import React from 'react';
import styled from 'styled-components';
import StatRow from './StatRow';

const Weapon = item => {
  return <WeaponWrapper>{/* <StatRow /> */}</WeaponWrapper>;
};

const WeaponWrapper = styled.div`
  display: grid;
`;
export default Weapon;
